FILE: third_party/blink/renderer/core/frame/navigator_id.cc

Run first to find exact method:
  grep -n "userAgent" ~/projects/chromium/src/third_party/blink/renderer/core/frame/navigator_id.cc

CHANGE A — Add include:
  #include "third_party/blink/renderer/modules/ant/ant.h"

CHANGE B — Add at top of userAgent() method body:
  String ant_brand = Ant::GetCachedBrand();
  if (!ant_brand.IsEmpty()) return ant_brand;
  // original code below
