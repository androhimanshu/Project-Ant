FILE: third_party/blink/renderer/modules/device_memory/navigator_device_memory.cc

CHANGE A — Add include:
  #include "third_party/blink/renderer/modules/ant/ant.h"

CHANGE B — Replace deviceMemory() body:
  float NavigatorDeviceMemory::deviceMemory() const {
    int32_t v = Ant::GetCachedValue();
    if (v > 0) return static_cast<float>(v);
    return ApproximatedDeviceMemory::GetApproximatedDeviceMemory();
  }
